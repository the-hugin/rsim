Use the `differential-review` skill from @~/.claude/skills/differential-review/SKILL.md

The target of the review is: $ARGUMENTS

If no arguments provided, review current staged/unstaged changes using `git diff HEAD~1` or `git diff --cached`.
